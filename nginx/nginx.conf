worker_processes  auto;
worker_rlimit_nofile 65535;

events {
  worker_connections 1024;
  use epoll;
  multi_accept on;
}

http {
  include       mime.types;
  default_type  application/octet-stream;
<<<<<<< Updated upstream
=======
  sendfile      on;
  server_tokens off;
>>>>>>> Stashed changes

  sendfile        on;
  tcp_nopush      on;
  tcp_nodelay     on;
  keepalive_timeout 65;
  server_tokens   off;

  # Security headers
  add_header X-Frame-Options         SAMEORIGIN   always;
  add_header X-Content-Type-Options  nosniff      always;
  add_header X-XSS-Protection       "1; mode=block" always;

<<<<<<< Updated upstream
  # Gzip
  gzip              on;
  gzip_vary         on;
  gzip_proxied      any;
  gzip_comp_level   5;
  gzip_types text/plain text/css application/json application/javascript text/xml image/svg+xml;

  # Rate limiting zones
  limit_req_zone $binary_remote_addr zone=api:10m  rate=60r/m;
  limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;

  # Upstreams
  upstream backend  { server backend:3000  max_fails=3 fail_timeout=30s; }
  upstream frontend { server frontend:80   max_fails=3 fail_timeout=10s; }
  upstream admin    { server gps-admin:80  max_fails=3 fail_timeout=10s; }

  # ── HTTP server — serves the app on port 80 ───────────────────
  # SSL is configured as an OPTIONAL add-on via Step 4 SSL script.
  # Do NOT redirect port 80 to HTTPS until a cert is actually installed.
=======
  limit_req_zone $binary_remote_addr zone=api:10m  rate=60r/m;
  limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;

  # ── HTTP — redirects to HTTPS when SSL is available ────────────
>>>>>>> Stashed changes
  server {
    listen 80;
    server_name _;

<<<<<<< Updated upstream
    # ACME challenge for Let's Encrypt cert issuance
=======
    # Let's Encrypt ACME challenge
>>>>>>> Stashed changes
    location /.well-known/acme-challenge/ {
      root /var/www/certbot;
    }

<<<<<<< Updated upstream
    # ── REST API ───────────────────────────────────────────────
    location /api/ {
      limit_req zone=api burst=30 nodelay;
      proxy_pass         http://backend;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
=======
    # Redirect everything else to HTTPS if cert exists; otherwise serve directly
    location / {
      return 301 https://$host$request_uri;
    }
  }

  # ── HTTPS ────────────────────────────────────────────────────────
  # Comment out this entire server block if you have no SSL certificate yet.
  # After getting a cert with certbot, uncomment and restart nginx.
  # server {
  #   listen 443 ssl;
  #   server_name yourdomain.com;
  #
  #   ssl_certificate     /etc/nginx/ssl/fullchain.pem;
  #   ssl_certificate_key /etc/nginx/ssl/privkey.pem;
  #   ssl_protocols       TLSv1.2 TLSv1.3;
  #   ssl_ciphers         HIGH:!aNULL:!MD5;
  #
  #   include /etc/nginx/conf.d/common_proxy.conf;
  # }

  # ── HTTP-only server (for initial setup without SSL) ─────────────
  server {
    listen 8080;
    server_name _;

    # Frontend (customer portal)
    location / {
      proxy_pass http://frontend;
      proxy_set_header Host              $host;
      proxy_set_header X-Real-IP         $remote_addr;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # REST API
    location /api/ {
      limit_req zone=api burst=30 nodelay;
      proxy_pass http://backend;
      proxy_set_header Host              $host;
      proxy_set_header X-Real-IP         $remote_addr;
      proxy_set_header X-Forwarded-Proto $scheme;
>>>>>>> Stashed changes
      proxy_read_timeout 30s;
      client_max_body_size 1m;
    }

<<<<<<< Updated upstream
    # Auth – stricter rate limit (must be after /api/ to override)
    location /api/auth/ {
      limit_req zone=auth burst=5 nodelay;
      proxy_pass         http://backend;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # Razorpay webhook — no rate limit (must match exactly before /api/)
    location = /api/billing/webhook {
      proxy_pass         http://backend;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # Health check — no rate limit, no auth
=======
    # Auth — stricter rate limit (overlapping prefix, must come AFTER /api/)
    location /api/auth/ {
      limit_req zone=auth burst=10 nodelay;
      proxy_pass http://backend;
      proxy_set_header Host              $host;
      proxy_set_header X-Real-IP         $remote_addr;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health endpoint — no rate limit
>>>>>>> Stashed changes
    location /health {
      proxy_pass       http://backend;
      access_log       off;
    }

<<<<<<< Updated upstream
    # Socket.IO — WebSocket upgrade
=======
    # WebSocket (Socket.IO) — long-lived connection
>>>>>>> Stashed changes
    location /socket.io/ {
      proxy_pass         http://backend;
      proxy_http_version 1.1;
      proxy_set_header   Upgrade    $http_upgrade;
      proxy_set_header   Connection "upgrade";
      proxy_set_header   Host       $host;
      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }

<<<<<<< Updated upstream
    # Admin panel — built with base="/admin/" so preserve the full path.
    # Using proxy_pass http://admin (no trailing slash) keeps /admin/... intact.
    location /admin {
      proxy_pass         http://admin;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # Customer portal — catch-all
    location / {
      proxy_pass         http://frontend;
      proxy_http_version 1.1;
      proxy_set_header   Host              $host;
      proxy_set_header   X-Real-IP         $remote_addr;
      proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
=======
    # Admin panel — accessible at /admin/
    location /admin/ {
      proxy_pass http://admin/;
      proxy_set_header Host              $host;
      proxy_set_header X-Real-IP         $remote_addr;
      proxy_set_header X-Forwarded-Proto $scheme;
>>>>>>> Stashed changes
    }
  }

  # ── HTTPS server ── uncomment AFTER running 04-ssl-setup.sh ──
  # server {
  #   listen 443 ssl;
  #   server_name yourdomain.com;
  #
  #   ssl_certificate     /etc/nginx/ssl/fullchain.pem;
  #   ssl_certificate_key /etc/nginx/ssl/privkey.pem;
  #   ssl_protocols       TLSv1.2 TLSv1.3;
  #   ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
  #   ssl_session_cache   shared:SSL:10m;
  #   ssl_session_timeout 10m;
  #   add_header Strict-Transport-Security "max-age=31536000" always;
  #
  #   # Copy all location blocks from the HTTP server above here
  # }
}

<<<<<<< Updated upstream
# ── TCP passthrough for GPS hardware ────────────────────────────
=======
# ── TCP passthrough for GPS hardware devices ─────────────────────────────────
>>>>>>> Stashed changes
stream {
  upstream gps_tcp { server tcp-server:5000; }
  server {
    listen 5000;
    proxy_pass            gps_tcp;
    proxy_timeout         120s;
    proxy_connect_timeout 10s;
  }
}
